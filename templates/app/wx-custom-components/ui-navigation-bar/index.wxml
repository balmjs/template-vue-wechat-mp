<wxs src="navigation-bar.wxs" module="nav" />

<view class="ui-navigation-bar{{ bgTransparent ? ' ui-navigation-bar--transparent' : '' }}" style="color:{{ color }};background-color:{{ bgColor }};{{ nav.linearGradient(isLinearGradient, sideOrCorner, colorStop) }}">
  <view class="ui-navigation-bar__placeholder" style="height:{{ statusBarHeight }}px;"></view>
  <view class="ui-navigation-bar__row">
    <view class="ui-navigation-bar__section--align-start">
      <image wx:if="{{ showGoBack }}" class="go-back" src="back.png" bindtap="onNavBack"></image>
      <slot></slot>
    </view>
    <view class="ui-navigation-bar__section">
      <text class="title">{{ title }}</text>
    </view>
  </view>
</view>
